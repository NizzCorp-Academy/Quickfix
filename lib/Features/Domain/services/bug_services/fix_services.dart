



class FixService {
  void fixBug(
    String tittle,
    String description,
    String date,
    String developer,
  ) {
    // Logic to fix a bug
    print("Bug fixed: $tittle, $description, $date, $developer");
  }

  

  // List<Fixmodel> getFixesForBug(String bugId) {
  //   return _fixes.where((fix) => fix == bugId).toList();
  // }
}